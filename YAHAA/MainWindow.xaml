<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="YAHAA.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:YAHAA"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="YAHAA">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Custom title bar (drag region set via SetTitleBar) -->
        <Grid x:Name="AppTitleBar" Grid.Row="0" Height="48" Background="Transparent">
            <StackPanel Orientation="Horizontal"
                        Spacing="12"
                        VerticalAlignment="Center"
                        Margin="16,0,0,0">
                <Border Width="22" Height="22" CornerRadius="6">
                    <Image x:Name="TitleBarLogo" Width="22" Height="22" Stretch="UniformToFill" />
                </Border>
                <TextBlock x:Name="AppTitleText"
                           Text="YAHAA"
                           VerticalAlignment="Center"
                           FontSize="13"
                           FontWeight="SemiBold" />
            </StackPanel>
        </Grid>

        <Frame x:Name="rootFrame" Grid.Row="1" />
    </Grid>
</Window>
